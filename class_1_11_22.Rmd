---
title: "class work 1/11/22"
author: "Izzy Bouklas"
date: "1/11/2022"
output: html_document
---
```{r}
library(tidyverse)
library(gssr)
library(broom)

data(gss_all)

gss2018 <- gss_all |> 
  filter(year == 2018) |> 
  janitor::remove_empty(which = c("rows", "cols")) |> 
  haven::zap_labels()

rm(gss_all)                     # remove big object
gc()

```

```{r}
d <- gss2018 |> 
  select(realrinc, tvhours, wrkstat) |> 
  drop_na() |>
  filter(wrkstat == 1)
```

descriptive stats ?????? wtf
```{r}
college = if_else degree <2 ???? what the fuck
```


Histogram:
```{r}
income_hist <- d |>
  ggplot(aes(x = realrinc)) +
  geom_histogram() 

income_hist
```


SUMMARY STATS
```{r}
tidy(d)
```

